---
name: kgx:ship
description: Create a portable OKF bundle for sharing the vault
disable-model-invocation: true
---

Create an OKF bundle (portable tarball of the vault).

1. Ask the user for the output bundle path (e.g. `bundle.okf.tar.gz`)
2. Run `kg ship --out <bundle-path>` via Bash
3. Show the created bundle path and contents
