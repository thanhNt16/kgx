---
name: kgx-ship
description: Create an OKF bundle for sharing the vault. Use when the user says "ship", "bundle", "package this vault", or "share my knowledge".
---

# kgx:ship

Create a portable OKF bundle from the vault.

```
kg ship --version <semver> --name "<name>"
```

Return the bundle file path and size.
