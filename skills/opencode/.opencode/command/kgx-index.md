---
description: Build or rebuild the SQLite brain index
---
Build or rebuild the brain index.

1. Run `kg index --full` via Bash
2. Wait for completion
3. Show the indexing result (note count, duration)
