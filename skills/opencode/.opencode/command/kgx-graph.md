---
description: Export the vault graph as an interactive 3D HTML visualization
---
Export the vault graph as an interactive 3D HTML visualization.

1. Run `kg graph --format html [--out <path>]` via Bash in the vault root.
2. Report the output path and the number of nodes/edges written.
3. Offer to open the generated HTML in a browser if the user wants to view it.
