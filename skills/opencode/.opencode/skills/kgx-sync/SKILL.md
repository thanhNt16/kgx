---
name: kgx-sync
description: Pull remote changes and reindex. Use when the user says "sync", "pull changes", or "update from remote".
---

# kgx:sync

Pull remote changes through git and reindex the brain.

```
kg sync
```

Return the sync summary (pulled changes, reindex results).
