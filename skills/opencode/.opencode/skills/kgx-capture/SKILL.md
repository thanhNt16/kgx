---
name: kgx-capture
description: Capture a raw source verbatim into the vault. Use when the user says "save this", "capture this", or pastes content to store immutably in raw/.
---

# kgx:capture

Capture raw source material verbatim (immutable).

```
kg capture --from <file|-> --type <doc|transcript|article|code>
```

Return the source note ID and file path.
