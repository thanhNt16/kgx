---
name: kgx-index
description: Rebuild the SQLite brain index with vector embeddings and community detection. Use when the user says "index", "rebuild", or after capture+extract.
---

# kgx:index

Build or rebuild the brain index with embeddings and communities.

```
kg index --full --communities
```

Return the index stats (note count, community count, elapsed time).
